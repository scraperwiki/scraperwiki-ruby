# A sample Gemfile
source "https://rubygems.org"

# Specify your gem's spec
gemspec

# NB This should be moved to gemspec as a dependency once sqlite_magic is
# stable and on rubygems
gem 'sqlite_magic', :git => 'git@github.com:openc/sqlite_magic.git'

